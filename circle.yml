machine:
  services:
    - docker
  environment:
    TERM: dumb
  java:
    version: oraclejdk8

dependencies:
  override:
    - ./gradlew resolveConfigurations -is

test:
  override:
    - ./gradlew build --info : { parallel: true, timeout: 300 }
